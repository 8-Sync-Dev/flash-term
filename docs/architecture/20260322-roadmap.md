# Roadmap — WezTerm Config Improvements

Last updated: 2026-03-22

## Status Legend
`[ ]` pending  `[~]` in-progress  `[x]` done  `[!]` blocked

---

## P0 — Stability / Correctness (fix before adding features)

| # | Task | File | Notes |
|---|------|------|-------|
| P0-1 | `[x]` Fix `EnumerationOptions` crash on PS 5.1 | `bootstrap.ps1` | replaced with `SearchOption` enum |
| P0-2 | `[x]` Fix spinner ANSI spam on SSH/tmux | `bootstrap.ps1` | width guard 20..300, init-time check |
| P0-3 | `[x]` Fix `lazygit` not found — missing extras bucket | `bootstrap.ps1` | `Ensure-ScoopBuckets` auto-adds extras |
| P0-4 | `[x]` Fix scoop update failing after fresh install | `bootstrap.ps1` | refresh PATH + verify before update |
| P0-5 | `[ ]` `wezterm.lua` — guard `pcall` on all `dofile` calls | `wezterm.lua` | crash if `current-bg.lua` malformed |
| P0-6 | `[ ]` Bootstrap: detect PS version, warn if < 5.1 | `bootstrap.ps1` | some machines run old WPS |

---

## P1 — Performance (startup speed)

| # | Task | File | Notes |
|---|------|------|-------|
| P1-1 | `[ ]` Lazy-load `Set-HistoryExperience` — defer PSReadLine import | `bootstrap.ps1` | saves ~80ms on slow machines |
| P1-2 | `[ ]` Lazy-load `zoxide init` / `starship init` — run async, apply when ready | `bootstrap.ps1` | both take 50-150ms each |
| P1-3 | `[ ]` Cache `Get-MissingPackages` result in `.state/` — skip on clean boot | `bootstrap.ps1` | avoid `Get-Command` × 16 on every tab |
| P1-4 | `[ ]` `wezterm.lua` — set `front_end = "WebGpu"` for GPU acceleration | `wezterm.lua` | fallback to OpenGL if unsupported |
| P1-5 | `[ ]` `wezterm.lua` — tune `animation_fps`, disable when on battery | `wezterm.lua` | reduce GPU load on laptops |

---

## P2 — UX / Features

| # | Task | File | Notes |
|---|------|------|-------|
| P2-1 | `[x]` `8sync clean --projects` — interactive stale repo picker | `bootstrap.ps1` | fzf multi-select, YES confirm |
| P2-2 | `[x]` `8sync clean --deep` — MCP/npm/pip/cargo/go artifact report | `bootstrap.ps1` | report-only, no auto-delete |
| P2-3 | `[x]` `8sync clean --scan` — Windows Defender quick + targeted scan | `bootstrap.ps1` | non-blocking, graceful skip |
| P2-4 | `[ ]` `8sync clean --projects` — add `git remote` column in fzf (origin URL) | `bootstrap.ps1` | helps identify which org a stale repo belongs to |
| P2-5 | `[ ]` `8sync bg` — auto-rotate wallpaper on timer (e.g. every 30min) | `bootstrap.ps1` | stored in `.state/`, toggle with `8sync bg rotate` |
| P2-6 | `[ ]` `8sync status` — show disk usage of `.state/`, `bg/` cache size | `bootstrap.ps1` | quick health overview |
| P2-7 | `[ ]` `8sync hx lang` — show installed vs missing status per language | `bootstrap.ps1` | color-coded table: green=ok, yellow=partial, red=missing |
| P2-8 | `[ ]` Tab completion: add `--days` value hint (e.g. `--days <N>`) | `bootstrap.ps1` | better discoverability |

---

## P3 — Code Quality / Maintainability

| # | Task | File | Notes |
|---|------|------|-------|
| P3-1 | `[ ]` Extract `8sync` subcommand dispatch into separate named functions | `bootstrap.ps1` | currently all inline in `Set-ToolAliases` |
| P3-2 | `[ ]` Move `$script:ToolPackages` and `$script:LangServers` to top-level config block | `bootstrap.ps1` | easier to update managed tool list |
| P3-3 | `[ ]` Add `#Requires -Version 5.1` at top | `bootstrap.ps1` | explicit minimum |
| P3-4 | `[ ]` `wezterm.lua` — split keybindings into separate `keys.lua` with `dofile` | `wezterm.lua` | file currently too long, hard to navigate |
| P3-5 | `[ ]` Write `docs/guides/20260322-8sync-commands.md` — full CLI reference update | `docs/` | current guide is outdated (pre clean/projects/deep/scan) |
| P3-6 | `[ ]` Write `docs/guides/20260322-troubleshooting.md` — known issues + fixes | `docs/` | document PS5.1 compat, ANSI spinner, fresh install steps |

---

## P4 — New Integrations

| # | Task | File | Notes |
|---|------|------|-------|
| P4-1 | `[ ]` `8sync clean --deep` — add `--delete` flag to actually remove artifacts | `bootstrap.ps1` | currently report-only by design; needs per-type confirmation |
| P4-2 | `[ ]` `8sync sync` — add `--check` dry-run that lists what would be installed/updated | `bootstrap.ps1` | useful before running on a new machine |
| P4-3 | `[ ]` `8sync hx` — auto-detect LSP binary presence, suggest install if missing | `bootstrap.ps1` | call `hx --health` and parse output |
| P4-4 | `[ ]` WezTerm: leader-key shortcut to trigger `8sync bg pick` in floating pane | `wezterm.lua` | e.g. `Leader + w` opens wallpaper picker inline |
| P4-5 | `[ ]` WezTerm: per-workspace background (different bg per named workspace) | `wezterm.lua` | store mapping in `.state/workspace-bg.json` |

---

## Tracking Notes

- Each item maps to one commit. Branch from `main`, PR optional.
- Priority order: P0 → P1 → P2 → P3 → P4
- When starting an item: change `[ ]` → `[~]`, commit the roadmap update first
- When done: change `[~]` → `[x]`, reference commit hash in Notes column
- Blocked items get `[!]` with reason in Notes

## Quick Start for Next Session

```
P0-5  wezterm.lua pcall guard     — 1 file, 30min
P1-1  lazy PSReadLine             — bootstrap.ps1, 45min
P1-3  cache Get-MissingPackages   — bootstrap.ps1 + .state/, 1h
P3-5  update 8sync CLI docs       — docs/guides/, 30min
```
