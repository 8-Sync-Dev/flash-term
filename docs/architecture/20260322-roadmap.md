# Roadmap — WezTerm Config Improvements

Last updated: 2026-03-22

## Status Legend
`[ ]` pending  `[~]` in-progress  `[x]` done  `[!]` blocked

---

## P0 — Stability / Correctness (fix before adding features)

| # | Task | File | Notes |
|---|------|------|-------|
| P0-1 | `[x]` Fix `EnumerationOptions` crash on PS 5.1 | `bootstrap.ps1` | replaced with `SearchOption` enum |
| P0-2 | `[x]` Fix spinner ANSI spam on SSH/tmux | `bootstrap.ps1` | width guard 20..300, init-time check |
| P0-3 | `[x]` Fix `lazygit` not found — missing extras bucket | `bootstrap.ps1` | `Ensure-ScoopBuckets` auto-adds extras |
| P0-4 | `[x]` Fix scoop update failing after fresh install | `bootstrap.ps1` | refresh PATH + verify before update |
| P0-5 | `[x]` `wezterm.lua` — guard `pcall` on all `dofile` calls | `wezterm.lua` | already done: `load_background_path` + `load_opacity` both use `pcall` + type check |
| P0-6 | `[x]` Bootstrap: detect PS version, warn if < 5.1 | `bootstrap.ps1` | `#Requires -Version 5.1` + runtime warn — `82fff12` |

---

## P1 — Performance (startup speed)

| # | Task | File | Notes |
|---|------|------|-------|
| P1-1 | `[x]` Lazy-load `Set-HistoryExperience` — drop slow `Get-Module -ListAvailable` | `bootstrap.ps1` | saves ~120ms/tab — `7644b5e` |
| P1-2 | `[x]` Lazy `zoxide init` / `starship init` via `Register-ShellEngineInits` — runs last | `bootstrap.ps1` | saves 100-300ms/tab — `82fff12` |
| P1-3 | `[x]` Cache `Get-MissingPackages` in `.state/missing-cache.json`, TTL 5min | `bootstrap.ps1` | saves ~200-400ms on clean boot — `7644b5e` |
| P1-4 | `[x]` `wezterm.lua` — `front_end = "WebGpu"` + `webgpu_power_preference = "HighPerformance"` | `wezterm.lua` | — |
| P1-5 | `[x]` `wezterm.lua` — fps already tuned (60/120); WebGpu unlocks full fps | `wezterm.lua` | done alongside P1-4 |

---

## P2 — UX / Features

| # | Task | File | Notes |
|---|------|------|-------|
| P2-1 | `[x]` `8sync clean --projects` — interactive stale repo picker | `bootstrap.ps1` | fzf multi-select, YES confirm |
| P2-2 | `[x]` `8sync clean --deep` — MCP/npm/pip/cargo/go artifact report | `bootstrap.ps1` | report-only, no auto-delete |
| P2-3 | `[x]` `8sync clean --scan` — Windows Defender quick + targeted scan | `bootstrap.ps1` | non-blocking, graceful skip |
| P2-4 | `[x]` `8sync clean --projects` — git remote origin column in fzf | `bootstrap.ps1` | normalized URL, 32-char truncation — `82fff12` |
| P2-5 | `[x]` `8sync bg rotate [on N\|off\|now\|status]` — auto-rotate via hidden BgRotate task | `bootstrap.ps1` | `.state/bg-rotate.json` — — |
| P2-6 | `[x]` `8sync status` — disk usage table (.state/, bg/, fonts/) + bg rotate state | `bootstrap.ps1` | — |
| P2-7 | `[ ]` `8sync hx lang` — show installed vs missing status per language | `bootstrap.ps1` | color-coded table: green=ok, yellow=partial, red=missing |
| P2-8 | `[ ]` Tab completion: add `--days` value hint (e.g. `--days <N>`) | `bootstrap.ps1` | better discoverability |

---

## P3 — Code Quality / Maintainability

| # | Task | File | Notes |
|---|------|------|-------|
| P3-1 | `[x]` Extract 8sync dispatch into `Register-8SyncAlias` | `bootstrap.ps1` | `82fff12` |
| P3-2 | `[x]` `$script:ToolPackages` and `$script:LangServers` already at top-level | `bootstrap.ps1` | lines 10-56 since initial commit |
| P3-3 | `[x]` Add `#Requires -Version 5.1` at top | `bootstrap.ps1` | `82fff12` |
| P3-4 | `[x]` `wezterm.lua` — keybindings split into `keys.lua`, loaded via `pcall(dofile)` | `wezterm.lua` + `keys.lua` | `0068954` |
| P3-5 | `[x]` Write `docs/guides/20260322-8sync-commands.md` — full CLI reference update | `docs/` | `86453f0` |
| P3-6 | `[x]` Write `docs/guides/20260322-troubleshooting.md` — known issues + fixes | `docs/` | 10 issues covered — `9d68821` |

---

## P4 — New Integrations

| # | Task | File | Notes |
|---|------|------|-------|
| P4-1 | `[ ]` `8sync clean --deep --delete` — per-type confirmation delete | `bootstrap.ps1` | plan: v0.5.0 G4 |
| P4-2 | `[ ]` `8sync sync --check` — dry-run list of missing/outdated tools | `bootstrap.ps1` | plan: v0.5.0 G5 |
| P4-3 | `[ ]` `8sync hx health` — parse `hx --health`, suggest missing LSPs | `bootstrap.ps1` | plan: v0.5.0 G7 |
| P4-4 | `[ ]` WezTerm: `Leader+w` opens `8sync bg pick` in floating pane | `wezterm.lua` | plan: v0.6.0 |
| P4-5 | `[ ]` WezTerm: per-workspace background via `.state/workspace-bg.json` | `wezterm.lua` | plan: v0.6.0 |

---

## P5 — Performance & GPU (v0.5.0)

| # | Task | File | Notes |
|---|------|------|-------|
| P5-1 | `[ ]` Smart GPU adapter auto-select (discrete on AC, integrated on battery) | `wezterm.lua` | plan: v0.5.0 G1 — via `wezterm.gui.enumerate_gpus()` |
| P5-2 | `[ ]` `webgpu_present_mode = "Mailbox"` + `webgpu_max_frame_latency = 2` | `wezterm.lua` | plan: v0.5.0 G1 — lower input lag |
| P5-3 | `[ ]` `animation_fps = 1` (was 60) — GPU idles between keystrokes | `wezterm.lua` | plan: v0.5.0 G2 — saves ~50mW |
| P5-4 | `[ ]` `freetype_load_target = "Normal"` (was "Light") — better ClearType | `wezterm.lua` | plan: v0.5.0 G2 |

---

## P6 — Deep Clean & Security (v0.5.0)

| # | Task | File | Notes |
|---|------|------|-------|
| P6-1 | `[ ]` cargo registry/src+cache + go clean-cache + docker prune in `Invoke-SystemClean` | `bootstrap.ps1` | plan: v0.5.0 G3 |
| P6-2 | `[ ]` nuget + yarn + pnpm cache in APP CACHES section | `bootstrap.ps1` | plan: v0.5.0 G3 |
| P6-3 | `[ ]` `8sync clean --audit` — npm audit + cargo audit + pip-audit pipeline | `bootstrap.ps1` | plan: v0.5.0 G6 |
| P6-4 | `[ ]` postinstall script scanner for malicious npm packages | `bootstrap.ps1` | plan: v0.5.0 G6 |

---

## Tracking Notes

- Each item maps to one commit. Branch from `main`, PR optional.
- Priority order: P0 → P1 → P2 → P3 → P4
- When starting an item: change `[ ]` → `[~]`, commit the roadmap update first
- When done: change `[~]` → `[x]`, reference commit hash in Notes column
- Blocked items get `[!]` with reason in Notes

## Quick Start for Next Session

Done session 1 (2026-03-22a): P0-5, P1-1, P1-3, P3-5
Done session 2 (2026-03-22b): P0-6, P1-2, P2-4, P3-1, P3-3, P3-4
Done session 3 (2026-03-22c): P1-4, P1-5, P2-5, P2-6, P3-2, P3-6 + mouse tracking fix + font fix
Planned session 4 (v0.5.0): P5-1..4 GPU, P6-1..4 deep clean/security, P4-1..3

See `docs/plans/` for detailed sprint plans per version.

```
v0.5.0 — GPU + deep clean + security (see 20260322-v0.5.0-gpu-clean-security.md):
  G1  smart GPU adapter + Mailbox present mode   — wezterm.lua, 30min
  G2  animation_fps=1, freetype Normal           — wezterm.lua, 15min
  G3  cargo/go/docker/nuget/yarn/pnpm clean      — bootstrap.ps1, 1h
  G4  --deep --delete with per-type confirm      — bootstrap.ps1, 1h
  G5  sync --check dry-run                       — bootstrap.ps1, 30min
  G6  --audit npm+cargo+pip vulnerability scan   — bootstrap.ps1, 1.5h
  G7  hx health LSP status                       — bootstrap.ps1, 45min
```
